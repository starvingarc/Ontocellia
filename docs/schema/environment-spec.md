# EnvironmentSpec

`EnvironmentSpec` defines the global task, spatial environment, fields, contact context, sources, scheduled events, and task translation zones.

Required top-level sections:

- `metadata`
- `global_environment`
- `spatial_environment`
- `task_translation`

Field initial patterns currently support `constant`, `x_gradient`, `y_gradient`, and `radial`.
